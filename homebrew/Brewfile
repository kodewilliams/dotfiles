# Set application directory
cask_args appdir: '/Applications'

# Install CLI tool to install applications
tap 'homebrew/cask'

# Install packages necessary for development
brew 'git'
brew 'tmux'
brew 'wget'
brew 'nvm'
brew 'yarn'
